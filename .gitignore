node_modules/
.next/
out/
.env
.env.local
.vercel
*.tsbuildinfo
next-env.d.ts

# Respaldos de los PDF antes de optimizarlos. No van al repositorio:
# el original siempre se puede recuperar del historial de git.
public/recursos/.originales/

# Configuracion local de herramientas, no del proyecto.
.claude/

# Documentos que sube el equipo desde el portal: tarjetas profesionales,
# certificados de estudio, consentimientos firmados. Son datos personales y
# `public/` es la carpeta que Next sirve al mundo entero sin pedir sesion:
# versionarlos aqui los publica dos veces, en el repositorio y en el sitio.
# Su sitio es un almacenamiento privado, no el arbol de git.
public/uploads/
